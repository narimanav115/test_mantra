# GitHub Repository Search App

A Flutter application that allows users to search GitHub repositories and manage a local favorites list using the GitHub REST API. Built with **Clean Architecture**.

## Build & Run

```bash
flutter pub get
flutter run
flutter test
```
